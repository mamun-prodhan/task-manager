import { Button } from 'flowbite-react';
import React from 'react';

const Login = () => {
    return (
        <div>
            <Button>
                Default
            </Button>

        </div>
    );
};

export default Login;